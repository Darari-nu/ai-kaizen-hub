# REQUESTS.md — 一撃実装仕様書

## トップの記事リストを間引く（2026-08-13）

darari指摘: 「記事が延々と続いているのでどこかで切った方がいい」→ 実装。

- **トップ `#article-list`**: 公開済み記事は全部表示、準備中(draft)は**各軸3本まで**。のこりは件数つきボタン「準備中もふくめて、記事一覧へ──のこり◯本」で `/articles/` に流す
- 公開記事が増えるとリストは実コンテンツで自然に育ち、準備中の羅列は増えない
- `/articles/` 一覧ページは従来どおり全件＋タブ絞り込み

UI変更はこのファイルの改訂とセットで行う（CLAUDE.md 技術方針）。
サイト全体の要件は `要件定義_draft_v0.md`、世界観は `BRAND.md` が正本。ここには**実装済みUIの仕様**を短く積む。

## トップページのモーション演出（2026-08-13）

darari指示: 参考サイト群（lusion.co / dogstudio.co / locomotive.ca ほか）を挙げて「かっこよくしたい」→ **見た目・世界観は真似ず、動きの質だけ拝借**で合意（BRAND.md「モーション」の項を同日改訂済み）。白ベース・エディトリアルのまま実装。

- **スクロール慣性**: Lenis を導入（依存追加はこれ1つ・数KB）。ホイール操作だけ慣性化し、タッチはネイティブのまま。`#articles` 等のページ内アンカーもLenis経由の滑らかな移動（絞り込みタブのクリック処理とは独立）
- **セクション入場リビール**: vanilla IntersectionObserver + CSSトランジション。`data-reveal` を付けた要素が視界に入るとフェード＋22px上昇（0.8s、editorialイージング）。**同じタイミングで入った要素は上から順に80ms刻みのスタガー**（記事リストの縦ズレ登場はこれで自動化）。3軸カードだけ `data-reveal-delay` で0/130/260msの固定ズレ
- **罫線が引かれる**: ペイン一覧・経歴・FAQの `border-t` は `.rule-draw` クラスで**左→右にスッと引かれる**（帳票の質感に合わせた、このサイト固有の演出語彙）
- **小ラベルの朱線**: SectionLabelの「──」がリビール時に左から伸びる（朱一点ルールの範囲内。SectionLabelは `data-reveal` 等を素通しできるようprops拡張）
- **ヒーロー**: ロード時1回だけ、キッカー→見出し→本文→CTA→右下タグラインが順に立ち上がる（120〜800ms刻み）。画像はスクロール連動の緩やかな視差（係数0.12、scale 1.05で縁を隠す。ヒーローが視界内のときだけ更新）
- **上質なホバー**: `main`/`footer` 内のリンク・ボタンの色/線/背景変化に0.25s ease を一律付与。画像ズームは0.3s→0.5s＋editorialイージングに変更
- 実装: `src/components/HomeMotion.astro`（トップ専用。他ページには読み込まない）。**`html.js` ゲートでJS無効時も全要素が見える**。`prefers-reduced-motion: reduce` で慣性・リビール・視差・ホバー遷移がすべて即停止（閲覧中の切り替えにも追従）
- 禁止事項は不使用: WebGL/3D・カーソル追従・ページ全体ピン留め・連続アニメーションなし。色・書体・直角の線は既存トークンのまま

## 記事一覧の3軸絞り込み（2026-08-10）

darari指示: 「記事、AIカイゼン、ガバナンスを選択したら、該当したものが出てくる、になってる？？」→ 実装。

- **トップ `#articles`**: 記事リストの上に「しぼりこみ」タブ（すべて／記録／AIカイゼン／ガバナンス）。クライアントサイドJSが `data-series` で表示を切り替える（静的サイトのままDB・ルート追加なし）
- **3軸カード**（記録/AIカイゼン/ガバナンス）はクリックで `#article-list` にスクロールし、同じ絞り込みが効く
- **「AIカイゼンできることメニュー」枠**は、記録/ガバナンスに絞ったときは非表示（すべて・AIカイゼンでは表示）
- **/articles/ 一覧ページ**にも同じタブ
- **今後の予告（comingSoon）**は文末の「（軸名）」からseriesを判定。無印は「すべて」のときのみ表示
- タブの見た目: 白ベース・エディトリアルの既存トークン（border-ink / text-nezu / font-display）。選択中= bg-ink×text-paper。角丸なし
