---
// セクション頭の小ラベル。枠なし・字間広めの静かな表示。
interface Props {
  text: string;
}
const { text } = Astro.props;
---

<span class="inline-flex items-center gap-2 text-[11px] tracking-[0.3em] text-nezu"
  ><span class="inline-block h-px w-6 bg-shu"></span>{text}</span
>
