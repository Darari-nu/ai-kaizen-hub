# 公開手順書 — darari-nu.com で AIカイゼンHP を公開するまで

作成: 2026-08-05 ／ 改訂: 同日（**ホスティングをGitHub Pages→Cloudflare Pagesに変更**。darari裁定「Cloudflare使いたい」）
作業者欄: 🤖=Claude(号令もらえばやる) / 👤=darari本人にしかできない

## なぜCloudflare Pagesか（変更の理由）

- 静的Astroサイトをそのまま無料ホスティング（月500ビルドまで無料・十分）
- 日本からの表示が速い（CDN）
- **Web Analytics無料つき** → 未決だった「分析ツール」問題がGA4なしで解決（クッキーレスなのでプライバシーポリシーも軽く済む）
- プレビューデプロイ（ブランチごとに確認URL）が標準
- GitHubリポは今まで通りソース置き場として使う（公開実例としてPublic維持）

## Phase A: リポジトリとCloudflare接続

| # | 作業 | 担当 |
|---|---|---|
| A1 | GitHubに `Darari-nu/ai-kaizen-hub`（Public）を作成し、このフォルダをpush | 🤖号令待ち |
| A2 | Cloudflareアカウント作成 or ログイン（無料プラン） | 👤 |
| A3 | Cloudflareダッシュボード → Workers & Pages → Create → **Pages** → Connect to Git → 上のリポを選択 | 👤(画面共有してくれれば🤖が誘導) |
| A4 | ビルド設定: Framework preset=**Astro** / Build command=`npm run build` / Output=`dist` | 👤(入力値はこの通り) |
| A5 | Deploy実行 → `https://<プロジェクト名>.pages.dev` で仮公開・表示確認 | 自動+👤 |

## Phase B: ドメイン接続（darari-nu.com）

2通りあります。**推奨はB-1（ネームサーバー移管）**——以後のDNS管理が全部Cloudflareで完結し、高速化・HTTPS自動です。

### B-1（推奨）: ネームサーバーをCloudflareへ

| # | 作業 | 担当 |
|---|---|---|
| B1 | Cloudflareに `darari-nu.com` をサイト追加（無料プラン）→ 表示される2つのネームサーバーを控える | 👤 |
| B2 | **Xserverの管理画面でネームサーバーをCloudflare指定の2つに変更** | 👤 |
| B3 | 浸透待ち（数分〜24時間） → Cloudflareが「Active」になる | 待つ |
| B4 | Pagesプロジェクト → Custom domains → `darari-nu.com` を追加（DNSは自動設定される） | 👤 or 🤖誘導 |

⚠️ darari-nu.com で現在何か運用中（メール含む）の場合、既存DNSレコードがCloudflareに自動コピーされるが、**移管前に既存レコード一覧のスクショを取っておく**こと。

### B-2（代替）: XserverのDNSのまま

- XserverのDNS設定で CNAMEレコード: `@`(またはwww) → `<プロジェクト名>.pages.dev` を追加
- ※apexドメインのCNAMEはXserver側の仕様次第。ダメならB-1へ

## Phase C: 公開直後にやること

- [ ] https://darari-nu.com 表示確認（スマホでも）
- [ ] OGP確認: X/DiscordにURLを貼ってカード表示を確認
- [ ] **Web Analytics有効化**: Pagesプロジェクト→Metrics→Web Analytics ON（クッキーレス。privacyページに「Cloudflare Web Analyticsを利用」の一文を追記=🤖やります）
- [ ] Lighthouse計測（PageSpeed Insightsに本番URL）
- [ ] Google Search Console 登録+sitemap送信（👤アカウント）
- [ ] SubstackのプロフィールにHPのURL掲載（👤）

## 更新の運用（公開後）

- 記事追加・修正 → mainにpush → Cloudflareが自動ビルド&デプロイ（1〜2分）
- ブランチを切ってpushすると本番に触らずプレビューURLが生える（大きな変更のとき用）
- 壊れたら: Cloudflareダッシュボードから前のデプロイに**1クリックでロールバック**（gitのrevertより速い）

## 退避メモ

- GitHub Pages用のCNAME・deploy.ymlは `drafts/github-pages-backup/` に退避済み（戻したくなったら復元可）
