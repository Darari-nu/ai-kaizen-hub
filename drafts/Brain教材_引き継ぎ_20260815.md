# 引き継ぎ: Brain教材第3期——Bot本番公開へ (2026-08-15)

新セッションは「この資料＋CLAUDE.md＋ここに書いたパスの資料」だけを前提に動けること。
商品・ローンチの確定事項は `drafts/Brain教材_引き継ぎ_20260813.md`、ヒアリング結果と
第2期の経緯は `drafts/Brain教材_引き継ぎ_20260814.md` が引き続き有効（重複記載しない）。
**制作の全記録（手順・詰まり・教材ネタ）の正本は `drafts/bot/制作記録.md`。必読。**

## 今やっていること

Brain教材の②Bot実装。**ダラリ重工業Bot（MVP=無料モードのみ）はローカルで完成し、
darari検品2回ぶんの修正も反映済み**。次は本番公開（darari-nu.comで動かす）。

## 状態

- 完了:
  - 要件定義v1: `drafts/bot/要件定義_ダラリ重工業Bot_v1.md`（裁定4件反映済み）
  - Worker実装一式: `/Volumes/DevSSD/Vibe_Website/260814_darari-jukogyo-bot/`（git init済み・**未コミット・GitHubリポ未作成**）
  - 規程コーパス23文書（HPのdist/company/から自動抽出。`npm run corpus` で再生成）
  - 忍者アバター `public/images/chatbot/avatar.png`（クリプト忍者#010.png由来・AI利用OK・image-gen-midori準拠で加工済み）
  - チャットウィジェット `src/components/ChatWidget.astro`（/company/配下のみ表示・Markdown描画・左上つまみでリサイズ可）
  - テスト5項目合格＋検品2回（詳細は制作記録の2026-08-14の項）
- 仕掛かり: 忍者の名前・キャラ設定が未決（現状「総務AI相談窓口」。BOT_NAMEとウィジェット表示名で差し替え可能）
- **次の一手: 本番公開。darariに着手の一言をもらってから順に**:
  1. GitHubに公開リポ作成（名前は仮 `Darari-nu/darari-jukogyo-bot`、darari承認済みの「新規公開リポ」裁定に基づく）→ push
  2. `npx wrangler kv namespace create BOT_KV` → 出たidを wrangler.toml に記入 → `npx wrangler secret put GEMINI_API_KEY` と `COOKIE_SECRET` → `npm run deploy`
  3. Cloudflareダッシュボードで Worker に `api.darari-nu.com` ルートを接続（DNSは既にCloudflare。darari立ち会いブラウザ操作が安全）
  4. HPリポをコミット・push（Cloudflare Pagesが自動ビルド）→ darari-nu.com/company/ で実地テスト

## このセッションで確定したこと（darari裁定）

- 技術構成: **新規公開リポ／Cloudflare Workers／Gemini無料枠（実費ゼロ）**
- **MVPは無料モード（1日3問）のみ**。本気診断（入社者限定モード）はPhase 2、連載の反応を見てから設計。ゲート方式もそのとき裁定
- 検品指摘の反映: 回答は結論先出し・箇条書き・250字目安（プロンプトの「回答スタイル」節）／パネルはリサイズ可／Markdown描画
- モデルは `gemini-3.5-flash`（2.5は新規ユーザー提供終了）＋ thinkingBudget:0
- 却下: Claude Haiku案（darariが実費ゼロを優先）／本気診断のMVP搭載

## ファイル・環境の状態

- **HPリポ 未コミット**: `src/layouts/Base.astro`（ウィジェット組み込み）／`src/components/ChatWidget.astro`／`public/images/chatbot/`／`drafts/bot/`一式／`#010.png`（素材、リポ直下）。コミットはdarariの指示待ち（従来運用どおり）
- Botリポ `.dev.vars`（gitignore済み）: GEMINI_API_KEY（プロジェクト`Dararijukogyo-BOT`のキー）＋COOKIE_SECRET＋**FREE_QUESTIONS_PER_DAY=100（ローカル開発専用の緩和。本番は3のまま）**
- ローカル起動: Botリポで `npx wrangler dev`（:8787）＋HPリポで `npm run dev`（:4321）→ http://localhost:4321/company/intranet/ 右下の忍者
- 前セッションのdevサーバは停止済み。新セッションで上記2コマンドを起動し直す

## 罠・注意（このタスク固有）

- HPリポに**APIキー・バックエンドを置かない**（DBなし・APIキーなし方針。画面だけ）
- 「1日3問」は仕様（入社導線）でありバグではない。darariが一度誤解したので説明の言葉を持っておく
- Astro devのホットリロード中はウィジェットが二重送信することがある（本番では起きない）
- wrangler.toml の compatibility_date はランタイム対応上限（2026-05-03）を超えない／KVのid欄が `REPLACE_ME_AFTER_KV_CREATE` のままなのでデプロイ前に必ず置換
- Gemini無料枠はプロジェクト単位。Botは専用プロジェクト`Dararijukogyo-BOT`を使う（他と混ぜない）。デプロイ後にレート制限ページでRPDを実測し `DAILY_GLOBAL_LIMIT`（現200）を調整
- 教材用スクショ（`drafts/bot/スクショ_*`）は公開前にメール・既存キー名等の**モザイク必須**
- 正式名は「ダラリ**重工業**」。Botの実装・原稿の記事化は `bunshou-kaizen-loop`→`kenpin-shitsu` フロー厳守

## 新セッション開始プロンプト

> /Volumes/DevSSD/Vibe_Website/260805_Darari-nu_HP/drafts/Brain教材_引き継ぎ_20260815.md を読んで、「次の一手」（Bot本番公開）から始めてください。
