# CLAUDE.md — ダラリーヌ公式HP「AIカイゼン」

このリポジトリは、だらリーヌ（darari）の**無料で学べる公式HP**を作るプロジェクト。

**🟢 方針決定（2026-08-05 darari裁定）: サイトは「AIカイゼン」全振り。**
製造業・会社員がAIを学べるサイトに一本化する。音楽（Dara/coban）・LINEスタンプは
最下部に「こんなのもあるぜ」程度の**おまけリンクのみ**（専用ページ・専用導線は作らない）。
Substackの記事も「会社員のためのAIカイゼン」に全振り。
**HPは倉庫、Substackが本丸** — 全ページからSubstack登録に誘導する。

## 一次資料（作業前に必ず読む）

| ファイル | 内容 |
|---|---|
| `BRAND.md` | **ブランドキット親レシピ（世界観の正本）**。色・文字・モチーフ・画像・文章の全規約 |
| `AIカイゼンHP_構想整理_20260805.md` | 8/2〜8/4ブレストの正本。旗・コンテンツ方針・軍議の裁定 |
| `要件定義_draft_v0.md` | 要件定義の素案（このリポジトリで育てて最終的にREQUESTS.mdに昇格させる） |
| `~/Claudecode/tama/memory/project_darari_hasshin_positioning.md` | 発信ポジショニングの一次記録 |
| `~/Claudecode/260307_2nd-Brain/00_システム/00_UserProfile/` | 本人プロフィール・文体ルールの正本 |
| `~/Claudecode/260307_2nd-Brain/43_JTC_AI推進室/` | 既存記事在庫（HPコンテンツの母体） |

## 旗（発信の核）— ブレさせない

> 「製造業の中の人が、会社にAIを"正規に申請→導入→使い倒す"実況」

- 本業＝製造部門のAI活用推進責任者（Claude Team導入、バイブコーディング教育計画）
- 前職＝全社AIガバナンス構築（倫理/セキュリティ/契約）
- 無料コンテンツは3軸：**記録**（僕のAI記録・実況）／**カイゼン**（道具型）／**ガバナンス**（現場語訳）。ネタの正本は `drafts/ネタ帳.md`
- 全自動生成コンテンツには**本人の一次体験・失敗談を必ず混ぜ込む**（軍議の裁定。素材は `40_だらリーヌ脳/sidebiz/asset_ledger_from_raw_logs_20260712.md`）

## 文体ルール（公開コンテンツ執筆時は厳守）

正本: `00_UserProfile/03_執筆スタイル(Style_Guidelines).md`

- 一人称は必ずひらがなの「**ぼく**」（「私」「僕」禁止）
- ペルソナ＝「AI操縦士のだらリーヌさん」親しみある熱血先輩調
- 読者の心の声を「」で先取り→返す／短文連打＋共感語尾／専門語は即※で平易解説
- 保険をかけない（「一概には言えませんが」禁止）／スタンスを取る
- **定番比喩の禁止**: 地図・羅針盤・土台・柱・筋トレ・DNA・車の両輪・潤滑油・エンジン・スパイス・レシピ
- 段落は20文字×3行が上限

※音楽（Dara Sound Work）レーンは真逆の文体（です・ます調／1文1行／太字禁止／売り込まない）。混ぜないこと。

**記事の固定フォーマット**（8ブロック構成＋機械検査＋公開手順）は `src/content/articles/_template.md`。
新規記事は必ずこの型で書き、失敗談は `asset_ledger_from_raw_logs_20260712.md` に裏付けがあるものだけ使う。
公開フロー: 執筆 → grep機械検査 → darari本人検品 → `draft: false` → push。

## 技術方針（既存公開サイトのパターンを踏襲）

- **Astro + Tailwind + GitHub Pages**（`Darari-nu/ai-reg-atlas`・`subsidy-watch` と同型）
- DBなし・APIキーなし。データは `data/*.yaml|json`、履歴はGit
- リポジトリ直下に `REQUESTS.md`（一撃実装仕様書）を置き、UI変更は仕様書改訂とセット
- 制作フロー: `/web-design-gpt` で要件定義 → Codexに実装依頼 → ビルド/デプロイはClaude側（Codexサンドボックスはネット遮断）
- デザイン: 「AIが作った感」回避4原則（均一カード禁止／実データの揺らぎ／余白に意図／角丸シャドウの既定値禁止）。AI Reg Atlasの金・音楽レーンの色トークンは**使わない**（ブランド分離）
- 独自ドメイン `darari-nu.com` 保有（Xserver）。接続設計は未決（下記）

## スキル

このリポジトリの `.claude/skills/` にシンボリックリンク済み（実体は `~/.claude/skills/`）:

- `web-design-gpt` — 要件定義の型。サイト設計はこれに沿う
- `codex-product-design` — UI/UXの実装・レビューをCodexに依頼するとき
- `pro-marketing-director`（軍配） — ファネル・LP・コピーの品質ゲート。公開前に必ず通す

このリポ限定のプロジェクトスキル（実体がこのリポにある）:

- `image-gen-midori` — **画像を生成するときは必ずこれを使う**。プロンプトはみどり脳の7要素で組み、Codex CLIで生成、検品まで（darariの方針: この種の知識はメモリーでなくリポ内スキルに置く）

リンク保留（必要になったら使う。実体は `~/.claude/skills/` にある）:

- `ai-company` — 戦略の壁打ち（イケハヤ部・西野部・サブスタ部）。グローバルで発動するのでリンク不要
- `meikyou-furedaiko` — 明鏡の紹介・アフィリエイト記事を書く段になったら
- `fable5-thinking-log` — Fable5に大きな設計判断を投げるときの申し送りログ

## 裁定済み（2026-08-05）

- **サイトはAIカイゼン全振り**。音楽・LINEスタンプは最下部のおまけ外部リンクのみ。専用ページ・フォロワー数掲載・専用リスト取りは一切しない
- これにより「人格分離ルールとの衝突」は解消（音楽公式サイト `260613_dara-sound-work` は別物として併存。あちらにだらリーヌを出さないルールも維持できる）
- 「Substackレーン問題」も解消: `dararinu.substack.com` は**会社員のためのAIカイゼンに全振り**。HPから取るリストはこれ一本
- **サイト構成は明鏡（ikedahayato.com/meikyou-official）が理想形**。トップは明鏡型ワンカラムLP構成の写像（要件定義STEP 2に対応表あり）
- **公開先は最初から `darari-nu.com`**（base='/'、public/CNAME、DNSは本人がXserverで設定）
- **デザインは白ベース・エディトリアル**（Less but Better。AI Reg Atlasの金・音楽トークンは不使用）

## 未決事項（勝手に決めない・本人に確認する）

1. **サイト名**: 「AIカイゼン」は再考中（本人がちょいダサ懸念）。名前は設定ファイル1箇所で差し替えられる実装にする
2. **リポジトリの正**: このSSDフォルダ vs `Darari-nu/ai-kaizen-hub`（GitHub公開リポ）の役割分担
3. **ドメイン設計**: `darari-nu.com` をこのサイトに当てるか、サブドメインか
3. ステップメール配信ツール（ConvertKit/Beehiiv/systeme.io）未選定
4. 無料コンテンツ候補5（動画編集）・6（HP自作）の具体的な代替手段
5. LINEスタンプは実績ゼロ → おまけリンクに載せるものが現状ない（準備中表記にするか、載せないか）
